let den: {
    id: number,
    datum: Date,
    pozn1?: string,
    pozn2?: string,
    kdo: number,
    
};
den = {
    id : 1,
    datum : new Date,
    pozn1 : "jojo",
    pozn2 : "nene",
    kdo : 1
}
console.log(den);

export default den;
